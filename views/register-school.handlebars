<style>
  .navbar {
    display: none;
  }
</style>

<div class="jumbotron container p-0 bg-white d-none d-md-block" style="margin-top: 5%; min-height: 500px; box-shadow: 0px 3px 20px 0px #0000000d; width: 65%;">
  <div class="row w-100 m-0 rounded" style="min-height: 500px;">

    <div class="col-sm-6 bg-white rounded-left pt-5 pl-5 p-0">
      <p><a href="/"><i class="fas fa-home"></i> Home</a></p>
      <div class="clippy rounded" style="-webkit-clip-path: ellipse(87% 49% at 31% 98%);
clip-path: ellipse(87% 49% at 31% 98%); top:0; left: 0; height: 100%; z-index: 0; background-color: #0d75e8de;"></div>

      <h1 class="text-left text-muted" style="font-weight: 200;">Code Assist</h1>
      <p class="pr-4 pt-3">All in one collaboration tool for students, mentors, and teachers.</p>

      <ul class="list-group text-right" style="position: absolute; bottom: 5%; right: 0px;">
        <li class="list-group-item mb-4 ml-auto border-0" style="border-radius: 30px 0 0 30px; width: max-content; box-shadow: -3px 6px 4px 1px #00000026; background: linear-gradient(to right, white, #ffffffdb);"><i class="far fa-paper-plane"></i> Responsive Mentors</li>
        <li class="list-group-item mb-4 ml-auto border-0" style="border-radius: 30px 0 0 30px; width: max-content; box-shadow: -3px 6px 4px 1px #00000026; background: linear-gradient(to right, white, #ffffffdb);"><i class="fas fa-people-carry"></i> Ask anyone in the Community</li>
        <li class="list-group-item mb-4 ml-auto border-0" style="border-radius: 30px 0 0 30px; width: max-content; box-shadow: -3px 6px 4px 1px #00000026; background: linear-gradient(to right, white, #ffffffdb);"><i class="fas fa-project-diagram"></i> Collaborate on Projects</li>
      </ul>

    </div>

    <div class="col-sm-6 pt-5 p-3" style="box-shadow: -4px 10px 9px 0px #0000000d;">
      <h1 style="font-weight: bold;">{{#unless updatingInfo}}Sign Up{{else}}Update School Info{{/unless}}</h1>
      {{#if schoolInvalid}}
        <div class="alert alert-warning alert-dismissible fade show" role="alert">
          Please select or enter a school name.
          <button type="button" class="close" data-dismiss="alert" aria-label="Close">
            <span aria-hidden="true">&times;</span>
          </button>
        </div>
      {{/if}}

      <div class="p-1 mb-4">
        <form method="post" action="/register-school">
          <div class="form-group">
            <label for="sel1">Are you a high school student?</label>
            <select class="form-control" id="high-school-student" name="student">
              {{#select high_school_student}}
              <option>Yes</option>
              <option>No</option>
              {{/select}}
            </select>
          </div>

          <div id="school-div" {{#if updatingInfo}}{{#unless school}}style="display: none;"{{/unless}}{{/if}}>
            <div class="form-group">
              <label for="sel1">Please choose the high school you attend</label>
              <!-- <p style="color: #696868; font-size: 13px; margin-bottom: 10px;"></p> -->
              <select class="custom-select {{#if schoolEmpty}}is-invalid{{/if}}" id="school" name="school">
                {{#select school}}
                <option value="">Select a school</option>
                <option>LASA High School</option>
                <option>Other</option>
                {{/select}}
              </select>
              <div class="invalid-feedback">Please select a school</div>
            </div>

            <div id="school-name-div" class="form-group mb-4" {{#if updatingInfo}}{{#unless other_school_name}}style="display: none;"{{/unless}}{{else}}style="display: none;"{{/if}}>
              <input id="schoolCustomInput" type="text" class="form-control p-3" placeholder="School Name" name="schoolCustom" value="{{other_school_name}}">
            </div>
          </div>

          <button type="submit" name="button" class="btn btn-primary btn-block btn-lg p-3 mx-auto mb-2" style="border-radius: 40px;">{{#unless updatingInfo}}Finish Sign Up{{else}}Save Changes{{/unless}}</button>
        </form>
      </div>

    </div>
  </div>
</div>

<div class="jumbotron container bg-white d-md-none">
  <p><a href="/"><i class="fas fa-home"></i> Home</a></p>
  <hr>

  <h1 style="font-weight: bold;">{{#unless updatingInfo}}Sign Up{{else}}Update School Info{{/unless}}</h1>
  {{#if schoolInvalid}}
    <div class="alert alert-warning alert-dismissible fade show" role="alert">
      Please select or enter a school name.
      <button type="button" class="close" data-dismiss="alert" aria-label="Close">
        <span aria-hidden="true">&times;</span>
      </button>
    </div>
  {{/if}}

  <div class="p-1 mb-4">
    <form method="post" action="/register-school">
      <div class="form-group">
        <label for="sel1">Are you a high school student?</label>
        <select class="form-control" id="high-school-student" name="student">
          {{#select high_school_student}}
          <option>Yes</option>
          <option>No</option>
          {{/select}}
        </select>
      </div>

      <div id="school-div" {{#if updatingInfo}}{{#unless school}}style="display: none;"{{/unless}}{{/if}}>
        <div class="form-group">
          <label for="sel1">Please choose the high school you attend</label>
          <!-- <p style="color: #696868; font-size: 13px; margin-bottom: 10px;"></p> -->
          <select class="custom-select {{#if schoolEmpty}}is-invalid{{/if}}" id="school" name="school">
            {{#select school}}
            <option value="">Select a school</option>
            <option>LASA High School</option>
            <option>Other</option>
            {{/select}}
          </select>
          <div class="invalid-feedback">Please select a school</div>
        </div>

        <div id="school-name-div" class="form-group mb-4" {{#if updatingInfo}}{{#unless other_school_name}}style="display: none;"{{/unless}}{{else}}style="display: none;"{{/if}}>
          <input id="schoolCustomInput" type="text" class="form-control p-3" placeholder="School Name" name="schoolCustom" value="{{other_school_name}}">
        </div>
      </div>

      <button type="submit" name="button" class="btn btn-primary btn-block btn-lg p-3 mx-auto mb-2" style="border-radius: 40px;">{{#unless updatingInfo}}Finish Sign Up{{else}}Save Changes{{/unless}}</button>
    </form>
  </div>

</div>

<script type="text/javascript" src="/js/register-school.js"></script>
